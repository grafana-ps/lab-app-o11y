{{- if .Values.order.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "otel-demo-apps.fullname" . }}-order
  namespace: {{ include "otel-demo-apps.namespace" . }}
  labels:
    {{- include "otel-demo-apps.componentLabels" (dict "root" . "component" "order") | nindent 4 }}
spec:
  replicas: {{ .Values.order.replicaCount }}
  selector:
    matchLabels:
      {{- include "otel-demo-apps.componentSelectorLabels" (dict "root" . "component" "order") | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "otel-demo-apps.componentLabels" (dict "root" . "component" "order") | nindent 8 }}
      annotations:
        {{- include "otel-demo-apps.instrumentationAnnotation" .Values.order.instrumentation | nindent 8 }}
        {{- with .Values.order.podAnnotations }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
    spec:
      containers:
        - name: order
          image: {{ include "otel-demo-apps.image" (dict "root" . "name" .Values.order.image.name) }}
          imagePullPolicy: {{ .Values.global.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          env:
            - name: OTEL_SERVICE_NAME
              value: "otel-demo-apps-order"
            # OTEL_RESOURCE_ATTRIBUTES is set by the OTel Operator Instrumentation CR
            # service.version comes from app.kubernetes.io/version label
            - name: PORT
              value: "8080"
            - name: PAYMENT_URL
              value: "http://{{ include "otel-demo-apps.fullname" . }}-payment:{{ .Values.payment.service.port }}"
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 15
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 10
            periodSeconds: 5
          resources:
            {{- toYaml .Values.order.resources | nindent 12 }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "otel-demo-apps.fullname" . }}-order
  namespace: {{ include "otel-demo-apps.namespace" . }}
  labels:
    {{- include "otel-demo-apps.componentLabels" (dict "root" . "component" "order") | nindent 4 }}
spec:
  type: {{ .Values.order.service.type }}
  ports:
    - port: {{ .Values.order.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "otel-demo-apps.componentSelectorLabels" (dict "root" . "component" "order") | nindent 4 }}
{{- end }}
